<Page
    x:Class="IPOkemonApp.MisPokemonPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IPOkemonApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!-- Fondo con imagen -->
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/Fondo_MisPokemon.png" Stretch="Fill"/>
        </Grid.Background>

        <!-- Definición de columnas y filas -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Logo -->
            <RowDefinition Height="2*"/>
            <!-- Contenido -->
        </Grid.RowDefinitions>

        <!-- Logo de la aplicación -->
        <Image Source="/Assets/logo_ipokemon.png" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" Margin="0,20"/>

        <!-- Botones de Pokémon -->
        <Border Grid.Column="0" Grid.Row="1" Background="Transparent" Margin="20">
            <Button x:Name="btnJigglypuffVAR" Click="btnJigglypuff_Click" Background="Transparent" BorderBrush="Transparent">
                <local:JigglypufVAR x:Name="JigglypuffControl"/>
            </Button>
        </Border>

        <Border Grid.Column="1" Grid.Row="1" Background="Transparent" Margin="20">
            <Button x:Name="btnRegiceVVG" Click="btnRegice_Click" Background="Transparent" BorderBrush="Transparent">
                <local:RegiceVVG x:Name="RegiceControl"/>
            </Button>
        </Border>

        <Border Grid.Column="2" Grid.Row="1" Background="Transparent" Margin="20">
            <Button x:Name="btnOddishJCS" Click="btnOddish_Click" Background="Transparent" BorderBrush="Transparent">
                <local:OddishJCS x:Name="OddishControl"/>
            </Button>
        </Border>
    </Grid>
</Page>

